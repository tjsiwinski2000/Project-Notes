**0902026**
## REACT ##
def get_weather(city:str):
    """Get Weather for a given city"""
    return "rainny"
	
agent = create_agent(
    model=llm,
    tools=[get_weather]
)

Now this is known as the react, which means reasoning.

So there is a reasoning part where the LLM tries to find out what function it has to

call based on the function doc strings.

And then act acting means it has to act to call something to call a function.

So react pattern, reasoning, action.

And now this part here, CTSTR, it's very important to provide this type hint here.

You can also do it without this.

But this is recommended because it makes it more clear to the LLM that this is a string argument.

So it knows that Rome should be sent to this as a string.

So the more specific you are with the docstrings, the better results you'll have.

The more accurate the AI agent will be. Because an AI agent is not a deterministic solution